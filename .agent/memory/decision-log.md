# Decision Log — Dulce Placer

| Fecha/Hora (UTC) | Agente | Qué se decidió | Por qué | Archivos afectados |
|---|---|---|---|---|
| 2026-09-08 13:22 | Claude (Cowork) | Se agregaron 3 métodos que faltaban en `NexusDatabase` (`tryGet`, `syncSet`, `syncDelete`) — el código ya los llamaba pero nunca se habían escrito. Cada llamada a `getAllPlanesDiarios`/`addPlanDiario`/`addOrdenProduccion`/`updateOrdenProduccion` tronaba (TypeError). | Esa falla reventaba el `Promise.all` en `useProduccionHook.ts` que carga formulaciones + modelosPan + planesDiarios juntos, por lo que la app SIEMPRE caía al fallback de solo-localStorage y nunca completaba la fusión/comparación con la nube. Esto explicaba por qué "Masa de Dulce Especial" (y sus insumos, y sus panes vinculados) mostraban cosas distintas en celular vs PC sin importar cuánto se sincronizara. Detalle completo: proyecto Claude "Panadería Dulce Placer" → `claude/auditoria-masa-dulce-especial-duplicada-dulce-placer.md`. AUTORIZO recibido de Gonzalo (mensaje "el 2autorizo", confirmando arreglo de fondo tras corregir el diagnóstico inicial). | `app/src/lib/database.ts` (protegido — AUTORIZO dado) |
| 2026-09-09 04:53 | Claude (Cowork) | **HALLAZGO CRÍTICO Y CORRECCIÓN:** se confirmó vía la configuración de Vercel (Settings → Git → "Directorio raíz" vacío) que el proyecto real que se despliega a `ma-13.vercel.app` es la carpeta RAÍZ del repo (`chalo72/Gestión-Sistema-Panadería`), NO la carpeta `app/`. El arreglo del 2026-09-08 se había aplicado dentro de `app/src/lib/database.ts`, una copia separada (con su propio `.git`) que nunca llega a producción. Se verificó que la copia real (`src/lib/database.ts`, raíz) tiene el MISMO bug (mismas 3 llamadas a `this.tryGet`/`this.syncSet`/`this.syncDelete` sin definir, líneas 560-565) y se aplicó el mismo arreglo ahí, adaptado al estilo de esa copia (que usa `SupabaseDatabase`/`_supabaseSync`, distinto del adapter genérico de `app/`). | El arreglo de ayer, aunque correcto en concepto, estaba en la copia equivocada y jamás habría llegado al celular. Esta es la copia que Vercel realmente construye — este es el arreglo que sí cuenta. Ver skill `verificar-repo-deploy-antes-de-editar` (creada tras un incidente anterior de este mismo tipo en este proyecto) y skill `metodo-fantasma-rompe-sync-dulce-placer`. | `src/lib/database.ts` (raíz del repo — protegido, mismo AUTORIZO ya dado el 2026-09-08 para este arreglo) |
