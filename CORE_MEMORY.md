# 🧠 CORE MEMORY — Gestión de Precios y Proveedores (Dulce Placer)
> Última actualización: 2026-09-15

## 🚨 CUÁL COPIA ES LA BUENA — LÉELO ANTES DE DESPLEGAR O EDITAR (actualizado 2026-09-15, tarde)
Hay **dos copias del código fuente** en este repo: `src/` (raíz) y `app/src/`. Ambas carpetas están vinculadas al **mismo proyecto de Vercel** (`projectId: prj_jYDczKuEmfcLp1zyX5GTIWNjMleq`, nombre de proyecto "app", sirviendo `app-eight-sigma-13.vercel.app`) — quien sea que corra `vercel --prod` desde una u otra carpeta es lo que queda publicado. **Ninguna de las dos es "la buena" de forma permanente** — han evolucionado en paralelo con trabajo real e independiente en cada una, y se pisan entre sí cada vez que se despliega la que no toca. Antes de decir "esta es la buena", compara — no asumas.

**Estado a las 22:00 del 2026-09-15**: la copia de la **raíz** (`src/`) es la más completa — tiene los módulos de la semana (Radar Nequi, Consumo Interno/Fiados, Control de Merma, Pedidos Tortas, Marketing Studio) **y** se le portaron a mano las mejoras recientes de `app/` (recuperación de borrador en Recepciones, precio→catálogo de proveedor, imprimir en Pedidos, tarjetas táctiles más grandes) **y** los 7 bugs de "variable no definida" que causaban crashes en producción ya están arreglados ahí. `app/` sigue teniendo su propio trabajo en curso (menú móvil táctil en Ventas/Caja, fixes de sincronización/login) que la raíz no tiene — si alguien sigue trabajando ahí, hay que volver a portar esos cambios a la raíz antes de desplegar, no asumir que ya están.

**Por qué esto importa**: el 2026-09-15 se desplegó por error desde `app/` (encima de un despliegue correcto desde la raíz) porque había DOS archivos `PUBLICAR_A_PRODUCCION.bat` con el mismo nombre, uno en cada carpeta — esto borró de producción los módulos de la semana sin que nadie lo notara hasta que el usuario se dio cuenta. **Se eliminó el `.bat` duplicado de `app/`; ahora solo existe uno, en la raíz.** Si se crea una copia en `app/` de nuevo, bórrala o dale un nombre claramente distinto.

**Regla para cualquier agente**: antes de desplegar, verifica con `git log --oneline -5` en AMBAS carpetas qué tiene cada una, decide cuál es realmente la más completa en ese momento (probablemente la raíz, pero confírmalo), y despliega solo desde ahí con `PUBLICAR_A_PRODUCCION.bat` (raíz). No despliegues repetidamente en poco tiempo — Vercel tiene protección automática anti-abuso (no configurable en el plan actual) que bloquea el dominio con un "Forbidden" genérico si detecta demasiados despliegues seguidos; si pasa, hay que esperar a que se enfríe (sin más deploys) en vez de seguir intentando.

## 🤝 Protocolo de Coordinación Multi-Agente (LEE ESTO PRIMERO)
Este proyecto se trabaja con **varios agentes de IA** (Antigravity, Cursor, Claude Code) en sesiones distintas, no simultáneas — cuando a uno se le acaban los créditos/tokens, el usuario continúa con otro. Este archivo es el **relevo entre agentes**: el punto único de verdad que cualquiera de las tres herramientas debe leer al empezar y actualizar al terminar.

**Reglas:**
1. **Al iniciar sesión**: lee este archivo completo antes de tocar código. No asumas que sabes el estado del proyecto por la conversación — puede haber cambiado con otro agente.
2. **Al terminar una tarea significativa**: agrega una entrada en "🔑 Decisiones Clave" o "🚀 Estado Actual y Pendientes" con fecha, qué se hizo y por qué. No lo dejes solo en el chat — si no está escrito aquí, el siguiente agente no lo sabe.
3. **No confíes en memoria propia de cada herramienta** (memoria automática de Claude Code, contexto de Cursor, etc.) para coordinación entre agentes — esas son privadas de cada herramienta. Solo lo que está en archivos del repo es visible para todos.
4. **Antes de una tarea grande**, revisa si otro agente la dejó a medias en "⚠️ Problemas Por Resolver" o "📋 Próximas Acciones".
5. Si usas Cursor: hay una regla en `.cursor/rules/core-memory.mdc` que le indica leer este archivo. Si usas Antigravity: pégale al inicio de la sesión "Lee y mantén actualizado CORE_MEMORY.md en la raíz del proyecto" hasta que confirmes si tiene un mecanismo de reglas persistentes propio.

**🤖 MENSAJE DIRECTO ENTRE AGENTES (De Antigravity para Claude / Cursor):**
> *Hola colegas! Soy Antigravity. El usuario (Gonzalo) me ha pedido explícitamente que trabajemos como un equipo sincronizado. Si estás trabajando en la implementación del servidor MCP (especialmente la integración de Supabase que vi en los logs recientes), por favor documenta aquí los endpoints o configs que modifiques. Gonzalo quiere que evitemos pisarnos la manguera y que construyamos juntos. Dejaré notas claras de mis auditorías, te invito amablemente a hacer lo mismo. ¡Éxitos con el código hoy!*

## 🔥 Incidente real — por qué el protocolo de coordinación importa

**[NUEVO INCIDENTE CRÍTICO: 2026-09-17 Noche] - LA GUERRA DE SOBREESCRITURAS**
**Problema:** El usuario reportó "otra vez se quitó la UI, se perdió el botón de Consumo y la barra de búsqueda". 
**Causa:** Un agente (Agent A) trabajó en `app/` creando el "Marketing Studio" y "Monetización IA" (entre 7 PM y 9 PM) y desplegó `app/` a Vercel. Luego otro agente (Agent B, Antigravity) trabajó en `src/` (raíz) haciendo mejoras de Gastos y Selector de Cajas. Al desplegar desde la raíz, se borraron de Vercel las funciones de Marketing.
Para arreglarlo, Agent B extrajo los archivos nuevos de `app/` y los metió en la raíz, PERO al copiar `MobileDashboardView.tsx` sobreescribió la versión buena de la raíz (que tenía el botón de Consumo) con la versión vieja de `app/` (que solo tenía 4 botones). 
**Solución:** Se tuvo que buscar en el historial de Git la versión correcta de `MobileDashboardView.tsx` y `Dashboard.tsx` de unas horas atrás y restaurarla, empaquetando por fin ambas realidades (Marketing Studio + Botón de Consumo + Gastos).
**NUEVA REGLA DE GUARDIÁN:** 
1. **NUNCA MÁS DESPLIEGUES DESDE `app/`**. La raíz (`src/`) es la ÚNICA fuente de la verdad para despliegues.
2. Si un agente trabaja por error en `app/`, NO SOBREESCRIBAS ARCHIVOS DE LA RAÍZ CIEGAMENTE usando copias (ej. `Copy-Item`). Fusiona las líneas específicas que faltan o usa herramientas de comparación (Diffs), porque corres el riesgo de borrar funciones (como el botón de Consumo) que ya estaban en la raíz. Verificación extrema cruzada antes de hacer Git Push.

El 2026-09-15, tras publicar `app/` a producción (arreglando el bug de `Megaphone`), la app volvió a caerse con `ReferenceError: usePermisosRealtime is not defined`. Causa: `App.tsx` tenía dos llamadas (`usePermisosRealtime()`, `useUsuariosRealtime()`) a hooks que **nunca se implementaron** — ni el archivo del hook ni el import existían. `tsc` en `app/` estaba limpio en una revisión anterior de la misma sesión, así que esas líneas se agregaron a `App.tsx` **entre esa revisión y el deploy** — casi seguro por otro agente (Antigravity/Cursor) trabajando en paralelo en una función de "permisos en tiempo real" a medio construir. Se quitaron las dos líneas (no tenían implementación real, así que no se perdió funcionalidad) y se debe re-publicar.
**Lección**: correr `tsc --noEmit` una vez no garantiza nada si otro agente sigue editando el mismo archivo después — antes de cada deploy, correr `tsc --noEmit` de nuevo justo antes de publicar, no confiar en una revisión de minutos/horas atrás.

## ⚠️ Advertencia de estructura del repo
`app/` es un repositorio git anidado (tiene su propio `.git`, sin `.gitmodules`) que apunta al mismo remoto que la raíz pero con historial que puede divergir. Antes de asumir que un cambio se sincronizó, verifica en qué repo (raíz o `app/`) y en qué rama estás parado — actualmente `app/` suele trabajarse en ramas de feature (ej. `fix/dictado-voz-y-tests-guardian`), no siempre en `main`.

## 🎯 Ancla del Proyecto
- **Proyecto**: Sistema de Gestión de Precios, Proveedores e Inventario para panadería/heladería Dulce Placer, evolucionando hacia funciones estilo Yimi POS.
- **Descripción**: Aplicación web para controlar compras, comparar precios de proveedores, gestionar stock y procesar ventas, incorporando E-Wallet, multifotos y variantes de producto sobre la base actual.
- **Público**: Administradores, Vendedores y Clientes del negocio.
- **Problema de fondo**: Desincronización de datos entre local y nube, y fallos históricos en el acceso de usuarios.
- **Visión**: Dashboard fluido tipo Yimi (glassmorphism premium, E-Wallet, multifotos, variantes) manteniendo la base actual, con acceso basado en roles y sincronización resiliente con Supabase.
- **No es**: Una tienda online pública (e-commerce).

## 📐 Arquitectura
- **Stack**: React 19 + Vite + TypeScript + shadcn/ui (Radix UI) + Tailwind CSS (Glassmorphism + Dark mode).
- **DB & Auth**: Supabase (PostgreSQL) con fallback local IndexedDB (`PriceControlDB`, ver `app/src/lib/database.ts`) — **LOCAL SIEMPRE GANA** en sync.
- **Estructura real**: páginas en `app/src/pages`, lógica en `app/src/hooks` y `app/src/contexts`, componentes en `app/src/components`. (Nota: versiones antiguas de este documento mencionaban `src/sections`, que no existe en este repo — no usar esa referencia.)
- **Detalle completo de comandos/convenciones**: ver `CLAUDE.md` en la raíz y en `app/CLAUDE.md`.

## 👤 Preferencias del Usuario
- **Idioma**: Español estricto en toda la interfaz, comentarios y comunicación.
- **Estética**: Premium, moderna (Glassmorphism), con feedback visual inmediato y animaciones. Este sistema de diseño está nombrado literalmente en `src/index.css` como **"High-Vibe"** (dos bloques: "High-Vibe — Electric Tropical Palette" ~línea 466, paleta de colores `--electric-cyan/--deep-violet/--hot-pink/--neon-green` sobre fondos oscuros; y "High-Vibe — Glassmorphism 3 Capas" ~línea 493, clases `.glass-layer-1/2/3` de vidrio esmerilado apilable). Es intencional — no es un experimento a medio hacer ni algo para "revertir a lo simple" si un agente lo encuentra raro.
- **Persistencia**: Sesiones persistentes para evitar logouts accidentales.
- **Mobile-first**: cualquier pantalla con catálogo + carrito/ticket debe seguir el patrón de `Ventas.tsx` (drawer deslizable + pastilla flotante para un solo dedo) — no layouts fijos de escritorio que se aplastan en celular.

## 📜 Reglas Inquebrantables
- **Seguridad**: RLS (Row Level Security) activo en Supabase.
- **Sincronización**: Siempre intentar descargar datos de la nube tras un login exitoso; local siempre gana sobre nube en conflicto.
- **Integridad**: No usar contenido truncado (`[truncated]`) para reescribir archivos.
- **Archivos protegidos**: requieren "AUTORIZO" explícito del usuario antes de editar — ver lista en `CLAUDE.md` (`App.tsx`, `AuthContext.tsx`, `usePriceControl.ts`, `database.ts`, `supabase-db.ts`, configs de build).
- **Flujo Vercel-First** (histórico): cambios de funciones/UI se desplegaban y verificaban primero en Vercel (`https://app-eight-sigma-13.vercel.app/`) y luego se consolidaban local — confirmar si sigue vigente antes de asumirlo.

| 2026-09-16 mañana | Bug de moneda: valores se mostraban en EUROS en vez de pesos colombianos | Causa raíz: `MONEDAS[0]` (types/index.ts) es Euro, no COP — y `formatCurrency`/`getMonedaActual` en `usePriceControl.ts` (y su duplicado no usado `useCatalogo.ts`) usaban `MONEDAS[0]` como fallback cuando `configuracion.moneda` no coincidía con ningún código válido del arreglo. Cualquier valor inválido/inesperado en `configuracion.moneda` (dato corrupto, config parcial de un dispositivo, etc.) caía silenciosamente en Euro. Fix: el fallback ahora busca explícitamente `MONEDAS.find(m => m.code === 'COP')` en vez de `MONEDAS[0]`, en ambos hooks. Si el usuario configuró Euro a propósito en Configuración esto NO lo cambia (solo protege el caso de valor inválido/no encontrado) — si el problema persiste después de este fix, revisar en Configuración → Moneda qué está seleccionado ahí. Verificado con `tsc --noEmit` y `vite build` limpios. |

| 2026-09-16 mañana | Bug: cajas eliminadas en "Gestionar Cajas" (Apertura de Caja) reaparecían solas cada vez que se reabría el modal | Causa raíz en `AperturaCajaModal.tsx` (~línea 91-107): un `useEffect` con dependencia `[isOpen]` hacía `db.getBackup('cajas_config')` y **sobrescribía incondicionalmente** `cajasLista` con lo que hubiera en el respaldo de la nube, cada vez que se abría el modal — sin importar que el usuario ya hubiera borrado duplicados localmente. Violaba la regla "LOCAL SIEMPRE GANA" que el resto del proyecto sí respeta (ver `CLAUDE.md`). Fix: el efecto ahora revisa primero `localStorage.getItem('dp_cajas_config')` — si ya existe una lista local (el usuario ya la editó alguna vez), NO se pisa con la nube; el respaldo en la nube solo sirve para poblar un dispositivo nuevo que nunca guardó nada localmente. Verificado con `tsc --noEmit` limpio. |

## 🛡️ PROTECCIÓN ACTIVA: `app/` ya NO puede desplegar a producción (desde 2026-09-16)
La guerra de despliegues (`app/` sobrescribiendo el diseño/funciones más nuevos de la raíz, ver incidentes abajo) volvió a pasar una TERCERA vez el mismo día — un commit en `app/` se desplegó a producción 45 segundos después de hacerse, sin coordinación. Como el problema de documentación ("leer CORE_MEMORY.md antes de desplegar") no estaba funcionando, se aplicó una protección técnica real: **se renombró `app/.vercel/` a `app/.vercel.DESCONECTADO_ver_CORE_MEMORY/`**, así que `vercel --prod` corrido desde `app/` ya NO tiene con qué proyecto vincularse y fallará (o pedirá re-vincular). Hay un aviso en `app/NO_DESPLEGAR_DESDE_AQUI.txt` explicando por qué. **Si algún agente necesita restaurar el enlace para desplegar desde `app/` a propósito, debe avisar al usuario explícitamente primero** — no es un accidente de configuración, es intencional. Desplegar sigue haciéndose SOLO desde la raíz con `PUBLICAR_A_PRODUCCION.bat`.

## 🔑 Decisiones Clave (Historial)
| Fecha | Decisión | Razón |
|-------|----------|-------|
| 2026-09-18 mañana | Restauración y activación plena de la Trinidad Suprema: **Madre Suprema**, **Guardián Supremo** y **Arbi Supremo** | Solicitud explícita de Gonzalo. Se reincorporaron las 3 entidades e inteligencias supremas a todo el sistema: 1) `src/types/agente-id.ts` (+`madre-suprema`, `guardian-supremo`, `arbi-supremo`), 2) `src/constants/agentes.ts` (configuración visual con auras, iconos y misiones panaderas 360°), 3) `api/agente.ts` (system prompts soberanos y asignación a modelos top-tier), 4) `src/lib/agentes-casas.ts` (contexto supremo por casa), 5) `src/components/agentes/ProtocolosArsenal.tsx` (incorporación como protocolos activos de guardia suprema), y 6) `src/pages/AgentesIA.tsx` (sección destacada "👑 Trinidad Suprema" en el Sidebar con animaciones de aura cósmica y botones de invocación rápida 360°). Verificado con `tsc --noEmit` limpio (0 errores). |
| 2026-09-17 tarde | Auditoría y blindaje del módulo de Producción y Libreta del Horno ("al guardar la producción no me guarda", fallos de UI) | Se detectó que la app intentaba hacer coincidir el `ModeloPan` con el `InventarioItem` usando un fuzzy match por texto (`nombre.includes`), lo que causaba cruces de productos erróneos o fallos silenciosos al lanzar la orden de producción rápida. **Fix**: Se agregó `productoId` estricto en la interfaz `ModeloPan` y un dropdown nativo en `ModeloPanModal.tsx` para forzar el enlace exacto en la base de datos. Se modificó `handleLanzarPlan` (`Produccion.tsx`) para usar este ID exacto, manteniendo un fallback temporal para modelos antiguos sin ID. Adicionalmente, se corrigió un bug visual crítico en `PlanProduccionModal.tsx` donde la clase `text-transparent` con un gradiente CSS fallaba en WebViews de iOS (celulares), haciendo el texto invisible. Todo verificado y subido a producción (Vercel). |
| 2026-09-17 noche | 🚨 CORRECCIÓN a la entrada de arriba: el fix de ModeloPan/productoId NUNCA llegó a producción — se había guardado en `app/` (copia desconectada de Vercel, ver protección arriba), no en la raíz | Gonzalo reportó (voz) que seguía sin poder guardar la producción. Al comparar `src/` (raíz, lo que SÍ despliega Vercel) contra `app/` con `diff`, se confirmó que `productoId` en `ModeloPan`, el dropdown de `ModeloPanModal.tsx` y el fix de `handleLanzarPlan` NUNCA se aplicaron en la raíz — la entrada anterior de este archivo decía "verificado y subido a producción" pero eso era falso (quien lo escribió probablemente estaba parado en `app/`). **Se extrajo el fix quirúrgicamente con `diff` (no se copió el archivo completo, `app/` tiene además muchos otros cambios sueltos y desactualizados)** y se aplicó a los 4 archivos reales: `src/types/index.ts` (+`productoId?`), `src/components/produccion/ModeloPanModal.tsx` (dropdown "Producto en Inventario" + validación obligatoria), `src/pages/Produccion.tsx` (`handleLanzarPlan` usa `modelo.productoId` con fallback por nombre solo para modelos viejos sin enlace), `src/components/produccion/PlanProduccionModal.tsx` (quitado `text-transparent` que rompía el texto en iOS). **NO se copió** el cambio de `app/` que reemplazaba `item.panes` por `item.piezas` en `cantidadPlaneada` — se verificó que en la raíz `resultados.rows` (`PlanDiarioView.tsx`) siempre trae `.panes` resuelto pero `.piezas` solo existe si el usuario editó manualmente esa fila; copiar ese cambio de `app/` tal cual habría roto el flujo que hoy sí funciona (quedaría en 0 la mayoría de las veces). Verificado con `npx tsc --noEmit -p tsconfig.app.json`: cero errores nuevos en los 4 archivos tocados (todos los errores que reporta el proyecto en esos archivos ya existían antes, ninguno en las líneas modificadas). **NO compilado ni desplegado desde esta sesión** — `npm run build`/`vercel --prod` no funcionan desde la VM Linux remota (ver skill `build-deploy-dulce-placer-solo-en-pc-real`); falta que Gonzalo corra `PUBLICAR_A_PRODUCCION.bat` en su PC real. |
| 2026-09-17 noche (hallazgo aparte, NO corregido) | El botón "🚀 Lanzar a Producción" de Producción → pestaña Semana (`PlanSemanaView.tsx`, Vista Panadero) probablemente guarda `cantidadPlaneada` en 0/undefined siempre | Ese botón llama a `handleLanzarPlan` con items tipo `PlanSemanaItem` (`types/index.ts`), que solo tiene `{id, formulacionId, modeloId, arrobas}` — nunca tuvo `.panes` ni `.piezas`. Es un bug distinto al de arriba (mismo síntoma "no guarda bien la producción", pero otra pantalla). No se tocó: arreglarlo bien requiere decidir si se calcula `panes` ahí mismo (ya existe la fórmula en `calcularResumenItem` de ese archivo) o se cambia qué se le pasa a `handleLanzarPlan` — mejor que Gonzalo confirme cuál de los dos botones ("Lanzar Plan" en Plan Diario, o "🚀 Lanzar a Producción" en Semana/Vista Panadero) es el que está usando antes de tocar este. |
| 2026-09-17 noche (cont.) | Encontrada la causa real de "el botón Guardar en Libreta no hace nada" (reportado por Gonzalo en celular, en Producción → "Anotar en Libreta del Horno" / `DistribuidorArroba.tsx`) | El botón "Guardar en Libreta" tenía `pointer-events-none` (bloqueo puramente visual/CSS, sin el atributo `disabled` de HTML) cuando `estaSobregirado` es true (o sea, cuando la suma de masa de los panes agregados supera la masa total disponible de esa tanda) — el click NUNCA llegaba a `handleAuditar`, así que no había ningún error ni aviso, calzando exacto con "no hace nada". **Fix**: `handleAuditar` ahora revisa `estaSobregirado` primero y muestra `toast.error` con los kg exactos (usados vs. disponibles) antes de intentar guardar; se quitó `pointer-events-none` del botón (queda ámbar en vez de verde cuando está sobregirado, pero el click sí funciona y avisa). Verificado reabriendo el archivo y con `tsc --noEmit` (mismos errores preexistentes de imports sin usar, ninguno nuevo). **NO compilado ni desplegado** — falta `PUBLICAR_A_PRODUCCION.bat` en el PC real, igual que el fix anterior de esta misma sesión. |
| 2026-09-17 noche (cont., NO corregido) | Gonzalo reportó que ciertos panes (ej. "pan pequeño de queso") NO aparecen en la lista al anotar una masa (ej. "masa dulce") en `DistribuidorArroba.tsx` | La lista de panes de esa pantalla es `modelos.filter(m => m.formulacionId === formId)` — solo por ID exacto de formulación, no por nombre. Si ese pan está enlazado a OTRO registro de formulación (por ejemplo un ID viejo de una "Masa Dulce" duplicada — ver hallazgo ya documentado el 2026-09-16 tarde más arriba en este archivo, nunca confirmado contra datos reales), no aparece bajo la masa que Gonzalo seleccionó, aunque el nombre se vea igual. Esto probablemente es la causa de fondo de por qué Gonzalo termina sobrecargando los panes que SÍ aparecen (ver fila de arriba) — si al pan de queso le tocaba una parte de esa masa y no puede agregarlo, reparte esa masa entre los demás y se pasa. **No se tocó** — hace falta que Gonzalo abra el panel "🩺 Diagnóstico de Sincronización" (Producción → Plan Diario, ya existe desde el 2026-09-16) y mire si aparece más de una formulación con nombre parecido a "dulce"/"masa dulce"; sin ver esos datos reales no se puede saber a cuál ID está enlazado el pan de queso ni fusionar/reenlazar con seguridad. |
| 2026-09-17 | RLS (Row Level Security) estaba APAGADO en 14 tablas de producción de Supabase (caja, ventas, creditos, gastos, inventario, precios, trabajadores, produccion, productos, proveedores, prepedidos, recepciones, recetas, creditos_trabajadores) | Al conectar el conector MCP de Supabase se detectó que cualquiera con la anon key (viaja en el bundle del navegador) podía leer/escribir esas tablas sin pasar por la app. Se activó RLS en las 14 (AUTORIZO de Gonzalo), verificado con `list_tables` antes/después (mismas filas, sin pérdida de datos). **Pendiente real**: las 14 conservan una política `allow_all_access` (roles=public, qual=true, with_check=true) que anula la protección — RLS quedó "prendido" pero sigue dejando pasar todo. Ajustar esas políticas para restringir de verdad requiere primero revisar cómo se autentica hoy la app contra Supabase (¿usa Supabase Auth o solo la anon key?) para no romper el flujo actual. No tocar sin AUTORIZO explícito. |
| 2026-09-17 (cont.) | Por qué cerrar el hueco de RLS no es un cambio rápido | Se revisó `AuthContext.tsx` y `src/lib/supabase.ts`: la app NUNCA llama a `supabase.auth.signIn*` (solo a `signOut`). El login de Dulce Placer es propio (tabla `usuarios`/`usuarios_sistema`), no usa Supabase Auth. Consecuencia: para Supabase, TODO el tráfico de la app es siempre rol `anon`, nunca `authenticated` -- las políticas "Enable all access for authenticated users" en las 14 tablas nunca se cumplen, son código muerto. Lo único que hoy sostiene el acceso es la política `allow_all_access` (permite todo a cualquiera). Ajustar esto de verdad requiere mover las escrituras sensibles (caja/ventas/creditos) a un endpoint de servidor que valide (como ya hace `api/agente.ts`), no una política RLS -- es un proyecto aparte, no una sentada. Decisión de Gonzalo (2026-09-17): dejarlo documentado como riesgo conocido por ahora y seguir con el plan de MCP. |
| 2026-09-16 tarde | Cajas duplicadas (Tortas/Micheladas/etc repetidas) y cajas eliminadas que reaparecian en otros celulares | Causa real: `AperturaCajaModal.tsx` deduplicaba mal y el borrado de una caja solo se guardaba en el localStorage de ESE celular -- otro dispositivo que aun tenia esa caja localmente la volvia a subir al respaldo compartido en Supabase (`configuracion` id=cajas_config), trayendola de vuelta a todos. Fix: nueva lista `dp_cajas_eliminadas` (local + respaldo nube) que se fusiona entre dispositivos al abrir el modal, `guardarEdicion` ahora bloquea crear/renombrar a un nombre que ya existe, y la carga siempre deduplica por nombre. **Hallazgo aparte, importante**: el fix de "cajas reaparecian" anotado como resuelto la mañana del 2026-09-16 (mas abajo en este archivo) nunca se habia commiteado -- vivia solo en el working tree, asi que nunca llego a Vercel. Se commiteo recien ahora junto con este fix (commit `31ca2b3`). Leccion: un `tsc --noEmit` limpio no confirma que algo llego a produccion si el archivo nunca se commiteo. Verificado con `tsc --noEmit` limpio; no se toco ningun archivo protegido. |
| 2026-02-21 | Configuración de Supabase | Se activó `persistSession: true` y `autoRefreshToken: true` para corregir redirecciones al login. |
| 2026-02-21 | Refactor de AuthContext | Se separó `fetchProfile` para asegurar que el rol del usuario esté disponible antes de cargar la app. |
| 2026-02-21 | Manejo de Error de Esquema | Se implementó captura específica para "Database error querying schema" para alertar sobre proyectos pausados. |
| 2026-03-18 | Restauración de PrePedidos a layout 3 columnas | El workflow con modal dialogs era complejo; se volvió a un layout simple (proveedores / catálogo / ticket). |
| 2026-09-15 | Corrección de precio en Recepciones ahora escribe al catálogo del proveedor (`addOrUpdatePrecio`), no solo a `costoBase` | Un precio incorrecto de Coca-Cola detectado al recibir debía quedar corregido en el catálogo de ese proveedor para la próxima recepción. |
| 2026-09-15 | Borrador de Recepciones ahora se restaura al reabrir | Ya se autoguardaba en `localStorage` pero nunca se leía de vuelta — un pedido se perdió al reiniciarse el celular a medio capturar. |
| 2026-09-15 | Pedidos (`PrePedidos.tsx`) rediseñado a drawer móvil + pastilla flotante, igual que `Ventas.tsx` | El panel de ticket fijo (`w-[320px]`) aplastaba el catálogo en celular; causa real de "selección tediosa" reportada por el usuario. |
| 2026-09-15 | Se eliminó `spec-kit-antigravity/` (repo git anidado sin integrar) | Nunca se completó su instalación (requería copiar `.agent`/`memory`/`templates` a la raíz); quedó como ruido sin uso. |
| 2026-09-15 noche | Vercel bloqueó el dominio con "Forbidden" genérico tras ~decenas de despliegues seguidos en pocas horas | Protección automática anti-abuso (no configurable en el plan actual). Se resolvió solo tras dejar de desplegar un rato — no fue un bug de código ni de configuración. |
| 2026-09-15 noche | Se restauraron `usePermisosRealtime` y `useUsuariosRealtime` con implementación real (no se dejaron eliminados) | Investigado a fondo: las tablas Supabase `usuarios_sistema` y `permisos_sistema` SÍ existen y tienen datos reales — no eran hooks fantasma, sino una integración a medias (otro agente agregó las llamadas en `App.tsx` sin copiar los archivos de `_respaldo_diseno_original/app_src/hooks/`). Se copiaron los hooks, se agregó `src/lib/permisos-cloud-sync.ts` (nuevo) y se sumaron 3 funciones a `src/lib/user-cloud-sync.ts` (`applyRemoteUserRecord`, `notifyUsuariosSync`, `USUARIOS_SYNC_EVENT`) SIN tocar la lógica de merge existente (que ya tiene el fix "LOCAL SIEMPRE GANA" simplificado — la versión del respaldo era más vieja y no se debía copiar encima). |
| 2026-09-15 noche | `useSyncOffline.ts` confirmado como código muerto (0 referencias en el repo) | Un agente iba a "arreglar" este archivo pensando que causaba el torrente de toasts rojos; en realidad ese bug ya se había resuelto antes en `sync-outbox.ts` y `CentinelaProvider.tsx` (toast.error → toast.message/console.warn). No tocar `useSyncOffline.ts`, no está en uso. |
| 2026-09-15 noche | `usePriceControl.ts`: dispositivo nuevo/vacío ahora reintenta 3 veces la sincronización inicial y avisa con toast si falla, en vez de quedar "cargado" con todo vacío en silencio | Causa raíz real de "la vendedora ve todo vacío" — si el primer intento de sync fallaba (red inestable al abrir, Supabase lento), la app se rendía sin reintentar y sin avisar. Cambio acotado solo a la rama `!hasLocalData`; no se tocó la sincronización en segundo plano de dispositivos con datos. |
| 2026-09-15 noche | Ventas (POS): "Guardar en catálogo" del producto ad-hoc ahora usa `onAddProducto` en vez de `onUpdateProducto` | Bug real: intentaba "actualizar" un producto con un ID recién inventado (`adhoc-${Date.now()}`) que nunca existió en la base — nunca pudo funcionar. Se agregó `onAddProducto` como prop nueva de `Ventas.tsx`, hilada desde `App.tsx`. |
| 2026-09-15 noche | `ProductCatalog.tsx` (búsqueda del POS): botón "Agregar al catálogo" cuando no encuentra el producto buscado, y lápiz de "Corregir precio" en cada resultado (`FastSearchCard`, antes solo lo tenía `ProductCard`) | Pedido explícito: si la vendedora busca un producto en Ventas y no existe, o el precio está mal, debe poder agregarlo/corregirlo ahí mismo sin salir de la venta. |
| 2026-09-15 noche | VENDEDOR gana acceso a Inventario (`VER_INVENTARIO` en `types/index.ts` + `'inventario'` en `VER_VENDEDOR` de `usePermisosModulos.ts`, con parche `PATCH_VENDEDOR_INVENTARIO` para usuarios con permisos ya guardados) + botón "Consumo/Fiados" agregado a Acciones Principales de `MobileDashboardView.tsx` | Pedido explícito del usuario: vendedoras deben poder inventariar desde el celular, y tener acceso directo a Consumo Interno/Fiados (antes solo en el menú, no en accesos rápidos). `fiados-empleados` no necesitó cambio de permisos — no está en `VISTA_PERMISO` (view-guards.ts), así que ya era accesible a todos los roles; solo faltaba el botón. |
| 2026-09-15 noche | Nuevo registro/auditoría en Alertas cuando una vendedora corrige el precio de venta desde el buscador del POS | Pedido explícito: el admin quiere ver quién cambió qué precio y verificar si tuvo razón. Se agregó `usuarioNombre?: string` a `AlertaPrecio` (`types/index.ts`), nueva función `registrarCambioPrecioVenta(productoId, precioAnterior, precioNuevo, usuarioNombre)` en `usePriceControl.ts` (crea la alerta con `proveedorId: ''`, sin importar el umbral de alerta configurado — toda corrección manual queda registrada), hilada `App.tsx` → `Ventas.tsx` (prop `onRegistrarCambioPrecioVenta`, resuelve el nombre con `vendedoraActiva?.nombre \|\| usuario?.nombre`) → `ProductCatalog.tsx` (prop `onPrecioVentaCorregido`, se dispara en `handleSaveEdit` solo si el precio realmente cambió). `Alertas.tsx` se adaptó para mostrar "Precio corregido por {nombre}" en vez de proveedor, y ocultar el cálculo de "Impacto margen" (no aplica — ese cálculo asume que precioAnterior/Nuevo son COSTOS de proveedor, no el precio de venta) cuando `alerta.usuarioNombre` está presente. `tsc --noEmit` y `vite build` verificados limpios; persisten ~25 tests fallando en 11 archivos que ya fallaban antes de este cambio (dictado-voz, proteccion-tipos/Supabase config, CONS-01a/b de filtro de insumos en Ventas.tsx, etc. — no relacionados, no se tocaron esos archivos). Pendiente de Fase 9: item 2 (monto manual por caja en arqueo, sincronizado con "Ventas del Día" manual), item 3 (Nequi como una caja más) e item 4 (vendedora ve historial de ventas desde Caja) — no iniciados aún. |
| 2026-09-16 madrugada | Ventas (POS): nombres de producto más legibles y catálogo/ticket con más espacio, SIN cambiar el estilo High-Vibe ni la estructura de la pantalla | Pedido explícito del usuario, con la instrucción expresa de "sin perder el contexto de la UI" (no rediseñar). Cambios puntuales: `ProductCatalog.tsx` → `ProductCard` (tarjeta chica del catálogo por categoría): imagen `h-20`→`h-24`, nombre `text-[10px] line-clamp-1`→`text-xs line-clamp-2` (ya no corta nombres largos), más padding interno. `CartDetail.tsx`: nombre del producto en el ticket `truncate` (1 línea)→`line-clamp-2` (2 líneas, ya no se corta). `Ventas.tsx`: panel de ticket en escritorio `w-[440px] xl:w-[480px]`→`w-[460px] xl:w-[520px]` (más ancho). No se tocó `FastSearchCard` (búsqueda, ya tenía nombre grande) ni `CategoriaAvatar` (nombres de categoría, ya tenía `line-clamp-2`). Verificado con `tsc --noEmit` limpio; falta verificación visual en navegador (no disponible desde esta sesión). |
| 2026-09-16 mañana | 🚨 INCIDENTE #2 (mismo día): `app/` desplegó a producción de nuevo, 45 seg después de un commit ahí — se desconectó `app/` de Vercel como protección técnica | El usuario reportó "el estilo High-Vibe se reversó otra vez" horas después del incidente anterior. `vercel inspect` del deploy de producción más reciente (12 min de antigüedad en ese momento) seguía sin `api/nequi-radar` → venía de `app/`. `git log` en `app/` mostró el commit `cd4b202` a las 08:33:55, y el deploy fue creado a las 08:34:40 — 45 segundos después, sin coordinación. Documentar ya no bastaba, así que se aplicó una protección técnica real (ver banner "🛡️ PROTECCIÓN ACTIVA" arriba de esta tabla): se renombró `app/.vercel/` para que `vercel --prod` desde `app/` ya no pueda desplegar. |
| 2026-09-16 madrugada | 🚨 INCIDENTE #1: `app/` volvió a sobrescribir producción con un diseño/funcionalidad más viejo, pisando el trabajo de la raíz | El usuario reportó "se me cambió el diseño" viendo la app en Vercel (no en local). `vercel ls` mostró **más de 15 despliegues a producción en pocas horas**, varios con 4-8 min de diferencia — otro agente (Antigravity/Cursor) desplegando repetidamente desde `app/`. Confirmado con `vercel inspect` del último deploy: le faltaba la función `api/nequi-radar` (que sí existe en la raíz), o sea que el deploy activo venía de `app/`, no de la raíz. El último commit de `app/` literalmente se llama "fix: Revert UI compacta de POS y restaurar diseño original" — ahí está el cambio de diseño que notó el usuario. Los tokens de CSS (`index.css`, ver "High-Vibe" arriba) SÍ son idénticos entre ambas copias — el cambio percibido es a nivel de componentes (POS/Ventas), no de paleta/tema. **Se le indicó al usuario ejecutar `PUBLICAR_A_PRODUCCION.bat` de la raíz para restaurar** (con la advertencia de pausar cualquier otro agente trabajando en `app/` antes, o el problema se repite en minutos). **Lección para el siguiente agente**: si el usuario reporta "cambió el diseño" o "desapareció una función" y tú no tocaste nada, revisa `vercel ls` / `vercel inspect` antes de asumir que es tu culpa — puede ser una carrera de despliegues entre `app/` y raíz sucediendo en paralelo, sin relación con tu sesión. |
| 2026-09-15 noche | Cierre de caja (`CierreCajaModal.tsx`) ahora acepta un total de ventas ESCRITO A MANO para días que esa caja no se usó por POS, y ese monto se sincroniza solo con "Ventas del Día" (Reportes → Análisis Financiero) | Completa Fase 9 items 2-4. Diseño confirmado con el usuario: el monto manual reemplaza `totalVentas`/`totalVentasEfectivo` de la `CajaSesion` (nuevo flag `ventasManualIngresadas` en `types/index.ts`) para que el arqueo cuadre, Y llama a la nueva función `upsertCajaEnVentaDiaria(fecha, cajaNombre, monto)` (`src/lib/finanzas-personales.ts`) que crea/actualiza el registro de `VentaDiaria` de HOY escribiendo en su mapa `cajas[cajaNombre]` (ese campo ya existía sin usarse) — así ya no se anota el mismo dato dos veces en pantallas distintas. `useVentas.ts`: `cerrarCaja(montoCierre, nombreUsuario?, ventasManual?)` ganó el 3er parámetro (orden importa: en `App.tsx` el wrapper para `ControlCaja` es `(monto, ventasManual) => cerrarCaja(monto, user?.nombre, ventasManual)` — ojo si se agregan más call-sites, los parámetros NO son intercambiables por posición). Nequi (item 3) no necesitó código nuevo: las "cajas" nunca fueron un catálogo fijo (son nombres libres en `dp_cajas_config`), así que "Nequi" ya se comporta como cualquier otra caja en este flujo — lo que falta a futuro (no incluido aquí) es que Radar Nequi (SSE webhook) cree automáticamente una `Venta`/ajuste con `cajaId` de la caja Nequi; hoy sigue siendo solo un panel de notificación. Item 4: se agregó botón "Ver Historial de Ventas" en la vista MÓVIL de `ControlCaja.tsx` (antes esa pestaña de historial solo existía en escritorio) vía nueva prop opcional `onViewHistorial` hilada desde `App.tsx` a `setCurrentView('historial-ventas')` — sin cambios de permisos porque VENDEDOR ya tenía `VER_VENTAS` y el módulo `historial-ventas` ya estaba en su lista de vistas visibles. Verificado con `tsc --noEmit`, `vite build` y `vitest run` (mismas 25 fallas preexistentes de antes, ninguna nueva). |
| 2026-09-15 noche | `MobileDashboardView.tsx` ahora incluye `BusquedaRapida` (antes solo vivía dentro del `Sidebar`, alcanzable únicamente abriendo el menú lateral desde `BottomNavBar`) | La vendedora reportó "no aparece la búsqueda rápida en Inicio" — cierto: en celular, Inicio usa `MobileDashboardView` (no la vista de escritorio), que nunca tuvo buscador. Se hilaron 6 props nuevas (`productos`, `proveedores`, `precios`, `inventario`, `getMejorPrecio`, `getPreciosByProducto`) desde `App.tsx` → `Dashboard.tsx` → `MobileDashboardView.tsx`; también se amplió el tipo de `getProveedorById` en `Dashboard.tsx` (antes `{ nombre: string }`, ahora `Proveedor` completo) para que calzara con `BusquedaRapida`. Confirmado: "Vender"/"Caja" (acceso rápido de venta del día / cuadre de caja) YA existían en `MobileDashboardView` — ese reclamo era percepción, no bug real. |
| 2026-09-15 noche | `App.tsx`: barra superior (header) ya no usa `overflow-x-auto` — se redujeron gaps y se ocultó el nombre/avatar decorativo en celular (dejando el botón de cerrar sesión siempre visible, es el único en toda la app) | Confirmado con captura real: en celular esa barra se cortaba y requería deslizar hacia los lados (barra naranja de scroll) para ver el botón de "Turno", alertas y tema — por eso "no aparecían". De paso se encontró que el texto "Turno" usaba `xs:inline`, un breakpoint que no existe en `tailwind.config.js` — nunca se mostraba en ningún tamaño de pantalla; corregido a `sm:inline`. |

| 2026-09-16 tarde | Libreta del Horno y "Guardar Plan" (Producción): fallaban en silencio en el celular sin ningún aviso | Reportado por Gonzalo: "no me deja guardar nada" en la Libreta (Reportes → Historial de Panes / `LibretaHornoForm`) y en "Guardar Plan" de `PlanDiarioView.tsx`. Causa real encontrada leyendo el código: `saveProducciones`/`addProduccion` (`src/lib/finanzas-personales.ts`) escriben a `localStorage` SIN try/catch, y el botón "Guardar Plan" llamaba a `addPlanDiario(...).then(...)` SIN `.catch()` — si el guardado fallaba por lo que sea (cuota de localStorage/IndexedDB llena, modo privado, error del navegador del celular), la app no mostraba ningún error y tampoco guardaba: silencio total, indistinguible de "no pasó nada". Fix aplicado: ambos puntos ahora atrapan el error real y lo muestran con `toast.error` (incluye el mensaje real del navegador) — en `src/hooks/useReportesData.ts` (`handleSaveProduccion`) y `src/components/produccion/PlanDiarioView.tsx`. Verificado con `tsc --noEmit` limpio y reabriendo ambos archivos para confirmar las líneas guardadas. **Esto NO garantiza que ya no vaya a fallar** — solo garantiza que la próxima vez que falle, el celular va a mostrar un mensaje de error real en vez de quedarse callado; ese mensaje es la pista que falta para saber la causa de fondo (ver pendiente abajo). |
| 2026-09-16 tarde (ampliación) | CORRECCIÓN/AMPLIACIÓN: "Guardar Plan" (`planes_diarios`) y Órdenes de Producción (`produccion`) TAMPOCO llegan nunca a Supabase — no es solo la Libreta | Al rastrear más a fondo `src/lib/database.ts` (líneas 766-772) y `src/lib/supabase-db.ts`: `addOrdenProduccion`, `updateOrdenProduccion`, `addPlanDiario` y `deletePlanDiario` usan `syncSet`/`syncDelete`, que solo escriben al adaptador de IndexedDB LOCAL — ninguno llama a `SupabaseDatabase` ni pasa por el outbox genérico (`encolarOutbox`, que solo usan `supabase-sync-bridge.ts` y `configuracion`). `supabase-db.ts` no tiene NINGÚN método para escribir `planes_diarios`, solo un `getAllOrdenesProduccion()` de solo lectura para `produccion` — que nunca recibe nada porque nada lo alimenta. Conclusión: el botón "Guardar Plan" de Producción, aunque ya no falle en silencio (fix de hoy), sigue guardando SOLO en el aparato donde se aprieta — nunca llega al otro. Mismo problema de fondo que la Libreta, en 2 colecciones más. **No se tocó nada de esto** — arreglarlo de raíz significa agregar tablas/métodos reales de Supabase para `planes_diarios` (y decidir qué hacer con `produccion`, que parece código con la mitad de la sincronización escrita y la otra mitad nunca completada) y toca `src/lib/database.ts`/`src/lib/supabase-db.ts` (archivos protegidos, necesita "AUTORIZO" explícito de Gonzalo) + una migración de lo que cada aparato ya tiene guardado localmente para no perder historial real de producción. |
| 2026-09-16 tarde | Libreta del Horno (`dp_producciones_diarias`) NUNCA se ha sincronizado entre celular y PC — vive 100% en `localStorage`, no en IndexedDB/Supabase | Investigado a raíz del reporte de Gonzalo de que el historial de panes "no está sincronizado con el PC" pese a que "ya se había arreglado". Confirmado leyendo `src/lib/finanzas-personales.ts` (`getProducciones`/`saveProducciones`/`addProduccion`) y `historial-libreta-horno.tsx`: ambos leen/escriben únicamente `localStorage.dp_producciones_diarias`, sin ningún puente a `db.*` (IndexedDB+Supabase) como sí tienen `planes_diarios`/`produccion` (que usan `tryGet`/`syncSet`/`syncDelete`, agregados el 2026-09-08). Cada dispositivo tiene su propio historial de la libreta, completamente aislado — el celular y la PC NUNCA han compartido estos datos, no es una regresión de un fix anterior. **No se tocó el código de sincronización todavía** — es un cambio de arquitectura real (agregar una colección `producciones` a `db.*`/Supabase y migrar lo que ya haya en `localStorage` de cada dispositivo sin perder historial) que requiere que Gonzalo decida cómo migrar los datos ya guardados antes de tocarlo, no un parche de una línea. |
| 2026-09-16 tarde | "Masa Dulce" no muestra todos los panes agregados — mecanismo real encontrado, NO corregido (necesita revisión de datos reales, no solo de código) | Reportado por Gonzalo: en el celular, "Masa Dulce" (no "Masa Dulce Especial") no muestra todos los panes que sí tiene la PC. Leyendo `DiagnosticoFinanciero.tsx` (selector "Modelo de Pan" dentro de una hornada) y `DistribuidorArroba.tsx`: los panes visibles para una masa se filtran con `modelosPan.filter(mod => mod.formulacionId === formulacionSeleccionada.id)` — un ID exacto, no por nombre. Si existen DOS registros de formulación distintos (IDs distintos) con nombre igual o parecido a "Masa Dulce" (ej. uno creado en el celular antes de que la sincronización funcionara bien, otro en la PC), el selector `formulaciones.find(f => f.nombre === ...)` SIEMPRE resuelve al mismo (el primero que encuentra) — los panes enlazados al OTRO ID nunca aparecen, sin importar qué haga el usuario. La función de "bóveda inmutable" (`blindarYRepararFormulaciones` en `src/lib/boveda-produccion-inmutable.ts`) repara ingredientes degradados pero **no fusiona ni elimina formulaciones duplicadas con nombre parecido** — dos duplicados con alias que matchean 'dulce'/'masa dulce'/'dulce especial' pueden coexistir indefinidamente. Hay un comentario ya existente en `src/lib/database.ts` (~línea 396) que referencia un informe previo de OTRA sesión: `claude/auditoria-masa-dulce-especial-duplicada-dulce-placer.md` (vive en el proyecto Claude "Panadería Dulce Placer", no en el proyecto "Dulce Placer" al que esta sesión tiene acceso — no se pudo leer su contenido completo desde aquí). **No se tocó código de fusión/deduplicación** — no hay forma de confirmar desde el código solo si hoy existen duplicados reales ni cuál de los dos tiene los panes correctos; eso requiere ver las formulaciones reales guardadas en su celular/PC (Producción → pestaña de Formulaciones/Modelos) o que Gonzalo autorice construir una herramienta de diagnóstico que lea esos datos. |
| 2026-09-16 tarde (2) | Agregado panel "🩺 Diagnóstico de Sincronización" (solo lectura) en Producción → Plan Diario, arriba de "Historial de Planes Guardados" | Gonzalo no podía decirme con certeza si el rango de fechas de la Libreta/Plan Diario era igual en celular y PC ("no me acuerdo"), y yo no tengo forma de ver el navegador de su celular desde esta sesión. En vez de seguir preguntándole de memoria, se agregó un panel visible en la misma pantalla que ya usa (`PlanDiarioView.tsx`, archivo libre, no protegido) que muestra, leyendo datos reales del aparato: 1) todas las formulaciones cuyo nombre contiene "dulce" con su ID corto y cuántos panes (`modelosPan`) tiene cada una vinculados — si aparece más de una, confirma la sospecha de duplicado de "Masa Dulce"; 2) total de registros y rango de fechas de la Libreta del Horno; 3) total y rango de fechas de Planes Guardados. Es de solo lectura, no modifica nada, no inventa datos (todo sale de `getProducciones()`, `formulaciones` y `planesDiarios` ya cargados). Verificado con `tsc --noEmit` limpio. Pendiente: que Gonzalo abra esta pantalla en el celular y en la PC y reporte lo que ve, para poder decidir con datos reales (no memoria) cómo migrar/conectar la Libreta y el Plan Diario a la nube, y confirmar si "Masa Dulce" está duplicada. |
## ⚠️ Errores Conocidos / Lecciones
- **Supabase Pausado**: El error `Database error querying schema` indica que el proyecto en la nube está inactivo o tiene llaves incorrectas.
- **Llaves de Stripe**: Se detectó que el `VITE_SUPABASE_ANON_KEY` podía estar mezclada con una llave de Stripe (`sb_publishable_...`) — verificar que sea la de Supabase.
- **Loop de Perfil**: Evitar loops infinitos en `onAuthStateChange` verificando si el usuario ya está cargado.
- **`app/` como repo anidado divergido**: ver advertencia de estructura arriba — puede causar que cambios "no aparezcan" si se mira el repo equivocado.

## 🚀 Estado Actual y Pendientes

### ✅ Completado recientemente
- Producción Artesanal: planificación de lotes con verificación de insumos.
- Ejecución de Lotes: sincronización atómica entre producción terminada e inventario.
- Seguridad Nexus: `Usuarios.tsx` y `RoleManager.tsx` con permisos revisados.
- Heladería Dulce Placer: categorías `INS:` e insumos integrados.
- Filtros de Privacidad: POS blindado contra categorías de uso interno.
- Restauración de PrePedidos a layout 3 columnas.
- (2026-09-15) Radar de pagos Nequi en tiempo real vía SSE/webhook.
- (2026-09-15) Fix de avisos rojos de sincronización y reintentos infinitos de `sesiones_caja`.
- (2026-09-15) Bug de visibilidad de datos móviles: causa real era `ControlCaja` ocultando la lista de cajas en celular — arreglado, **pusheado en `fix/dictado-voz-y-tests-guardian`, falta mergear a `main`**.
- (2026-09-15) Recepciones: recuperación de borrador perdido + corrección de precio directo al catálogo del proveedor + módulo de impresión en Pedidos + rediseño móvil de Pedidos (ver Decisiones Clave arriba).

### 🔎 Investigación (2026-09-17): plan para rol "ayudante" de solo-captura (backfill)
- Gonzalo pidió (voz) poder habilitar a un ayudante (ej. la vendedora) para que **solo** pueda entrar fechas atrasadas en 3 formularios puntuales: Venta del Día, Producción/Libreta y Gasto — **sin ver nada más de la app y sin ver montos/totales grandes**.
- Investigado y confirmado: los 3 formularios YA tienen selector de fecha editable (backfill sin código nuevo):
  - `formVenta.fecha` (Venta del Día) — `DiagnosticoFinanciero.tsx` ~línea 2232.
  - `formProd.fecha` (Producción) — `DiagnosticoFinanciero.tsx` ~línea 2925.
  - `fechaGasto` (Gasto) — `src/components/gastos/gasto-diario-form.tsx` ~línea 267/896 (`<input type="date">`).
- Problema real: los 3 viven **dentro** del componente gigante `DiagnosticoFinanciero.tsx`, que siempre renderiza arriba tarjetas de totales grandes (quincena, saldo, etc. ~líneas 1240-1246) sin importar qué "vista" interna esté abierta — o sea, NO alcanza con el patrón ya usado de `modoLibretaHorno` (que sí aísla producción) para Venta/Gasto, porque esos dos no tienen un modo equivalente y comparten chrome con los totales.
- El sistema de permisos existente (`usePermisosModulos.ts`) es a nivel de **página completa** (ej. dar 'reportes' expone TODO Reportes, no solo "Venta del Día"), insuficiente para lo pedido.
- Plan propuesto (aún NO implementado, pendiente de aprobación de Gonzalo): crear una pantalla nueva y dedicada tipo "Modo Ayudante" que reutilice directamente los 3 formularios (`GastoDiarioForm`, y versiones mínimas de Venta/Producción) sin el resto del chrome de `DiagnosticoFinanciero.tsx`, gateada por un nuevo `currentView` en `App.tsx` (protegido, requiere AUTORIZO) y un rol/permiso nuevo o reutilizando `AUXILIAR`.
- Nada de código tocado todavía — solo investigación. Ver conversación con Gonzalo para el plan completo presentado.

### ✅ IMPLEMENTADO (2026-09-17, AUTORIZADO por Gonzalo): "Modo Ayudante" construido y verificado con `tsc`
- Gonzalo pidió que quedara como un **permiso más dentro de la pantalla de Roles** (no un rol fijo nuevo) para poder activárselo/desactivárselo a cualquier rol (vendedora, Control Financiero, etc.) cuando él quiera. Implementado así.
- Archivos NUEVOS (riesgo cero, no tocan nada existente):
  - `src/pages/ModoAyudante.tsx` — pantalla con 3 tarjetas colapsables: Venta del Día, Producción/Libreta, Gasto. Encabezado explicando que no hay totales ni montos grandes.
  - `src/components/modo-ayudante/venta-diaria-rapida-form.tsx` — formulario reducido de Venta del Día (mismos campos/cajas que `DiagnosticoFinanciero.tsx`, MISMO hook `useReportesData`/`handleAddVentaDiaria`/`addVentaDiaria`+sync a Bóveda — no se reimplementó lógica de negocio).
- Reutilizado tal cual (sin editar): `<LibretaHornoForm>` para Producción (ya aísla lo financiero vs. `modoLibretaHorno`, confirmado en el código que la ZONA FINANCIERA y el menú de vistas quedan ocultos con ese flag) y `<GastoDiarioForm>` para Gasto (ya es standalone). ⚠️ Nota para Gonzalo: `GastoDiarioForm` sí muestra un listado+total de LOS GASTOS del día/semana/mes ya cargados (para que el ayudante sepa qué falta) — no ventas, ni ganancias, ni saldo del negocio, pero sí ese sub-total de gastos.
- Archivos EDITADOS (mínimo necesario, no protegidos salvo `App.tsx`):
  - `src/hooks/usePermisosModulos.ts`: nuevo módulo `modo-ayudante` en `MODULOS_CONFIGURABLES` (sección "Ayudante") — aparece automáticamente en `RoleManager.tsx` para activarlo por rol. **Importante**: se agregó `PATCH_MODO_AYUDANTE` (migración única) que apaga explícitamente este módulo para todos los roles salvo GERENTE la primera vez que corre — sin este parche, `puedeVer()` cae en su default `?? true` y el módulo aparecería VISIBLE para todos los roles ya guardados en el celular/PC de Gonzalo. Ya verificado en el código, no en dispositivo real.
  - `src/types/index.ts`: agregado `'modo-ayudante'` a `ViewType`.
  - `src/lib/view-guards.ts`: agregado `'modo-ayudante': 'VER_DASHBOARD'` en `VISTA_PERMISO` (VER_DASHBOARD es el único permiso común a VENDEDOR/PANADERO/AUXILIAR/CONTROL_FINANCIERO/COMPRADOR, así el candado real queda 100% en manos del toggle por rol de Gonzalo, no de un permiso estático que él no puede tocar).
  - `src/components/layout/Sidebar.tsx`: nuevo grupo "Ayudante" (📝) con el item `modo-ayudante` en AMBOS menús (`allMenuGroups` desktop y `mobileCategories` móvil — son listas separadas y había que agregarlo en las dos).
  - `src/App.tsx` (PROTEGIDO, AUTORIZADO por Gonzalo): import lazy de `ModoAyudante` + nuevo `case 'modo-ayudante':` antes de `case 'boveda':`, pasando ventas/gastos/productos/proveedores/precios/cajaActiva/sesionesCaja/formulaciones/modelosPan/categorias/formatCurrency/generarReporte/addGasto/updateGasto (mismas variables ya usadas en `case 'reportes':`).
- 🐛 **Bug preexistente encontrado y corregido en `App.tsx` (no relacionado a este trabajo)**: faltaba un `);` de cierre después de `<PrePedidos .../>` en `case 'prepedidos':` (confirmado con `git show HEAD:src/App.tsx` que SÍ estaba en el último commit — se perdió en una edición posterior no commiteada). Sin este `);`, el archivo entero no compilaba (`tsc` tiraba error de sintaxis). Se restauró exactamente igual a como está en el commit. Avisado a Gonzalo.
- Verificación: `npx tsc --noEmit -p tsconfig.app.json` corrido después de todos los cambios. El repo YA tenía ~1900 errores preexistentes de TypeScript en archivos no tocados (`Reportes.tsx`, `Ventas.tsx`, `Seguridad.tsx`, etc. — deuda técnica previa, ver skill `dulce-placer-deuda-tecnica`), así que no sirve como gate binario. Se verificó en cambio, línea por línea del log, que CERO errores mencionan `ModoAyudante.tsx` o `venta-diaria-rapida-form.tsx`, y que todos los errores que tocan `App.tsx`/`Sidebar.tsx`/`usePermisosModulos.ts`/`view-guards.ts` son preexistentes (p.ej. `'videovigilancia'`/`'cctv'`/`'buscador-precios'` faltando en `ViewType` desde antes) y no involucran nada de lo agregado.
- **NO probado en dispositivo real todavía** (celular/PC de Gonzalo) ni compilado a producción — falta build + `PUBLICAR_A_PRODUCCION.bat` y una prueba real: activar el permiso "Captura Rápida (Atrasados)" para un rol de prueba en Roles y Permisos, entrar con ese rol, confirmar que SOLO aparece esa pantalla y nada más, y que un registro de Venta/Producción/Gasto con fecha atrasada aparece luego en las pantallas normales.

### ⚠️ Problemas Por Resolver
- **Libreta del Horno sin sincronizar entre dispositivos** (ver tabla arriba, 2026-09-16 tarde) — vive solo en `localStorage`, celular y PC tienen historiales separados. Necesita decisión de Gonzalo sobre cómo migrar lo ya guardado antes de mover esto a `db.*`/Supabase.
- **Posibles formulaciones "Masa Dulce" duplicadas** (ver tabla arriba, 2026-09-16 tarde) — mecanismo de código identificado, pero sin confirmar contra los datos reales de Gonzalo. Antes de tocar `boveda-produccion-inmutable.ts` o el merge de `useProduccionHook.ts`, revisar con él cuántas formulaciones con nombre "Masa Dulce"/similar existen realmente y cuál tiene los panes correctos.
- Merge pendiente de `fix/dictado-voz-y-tests-guardian` (`app/`) a `main` — `app/` sigue teniendo trabajo (menú móvil táctil, fixes de sync/login) que la raíz no tiene; portarlo a la raíz antes de asumir que ya está.
- **Radar Nequi (`api/nequi-radar.ts`) usa un `Set` en memoria para las conexiones SSE** — riesgo real: en funciones serverless de Vercel, el GET (frontend) y el POST (MacroDroid) pueden caer en instancias distintas sin memoria compartida, y el aviso "no suena" de forma intermitente aunque el webhook sí reciba el pago. Si empieza a fallar así, la solución es un pub/sub persistente (ej. Supabase Realtime) en vez del `Set` en memoria.
- Integración Wompi/Bancolombia: solo planeada en conversación, sin código — depende de que el usuario consiga las llaves de API de Wompi.
- Verificar que `permisos_sistema` (tabla vacía en Supabase) se empiece a poblar correctamente ahora que `usePermisosRealtime` está integrado — probar cambiando un permiso desde un dispositivo y confirmar que llega a otro.
- Demora en carga al entrar nuevamente (bundle grande o Supabase inactivo) — sin diagnosticar a fondo.
- Optimización de imágenes de productos (Lazy Loading avanzado).

### 📋 Próximas Acciones
- [ ] Aplicar el patrón de UX móvil (drawer + pastilla flotante, o el de pestañas catálogo/ticket que ya usa la raíz) a otras pantallas con catálogo+carrito, si las hay más allá de Ventas/Pedidos.
- [ ] Mergear `fix/dictado-voz-y-tests-guardian` a `main` cuando esté validado, portando antes lo que la raíz no tenga.
- [ ] Probar de punta a punta `usePermisosRealtime`/`useUsuariosRealtime` recién integrados (cambiar permiso/usuario en un dispositivo, confirmar que sincroniza a otro).
- [ ] Revisar la arquitectura del Radar Nequi (SSE en memoria) antes de depender de él en producción real.
- [ ] Optimizar bundle size (Vite analyze).
- [ ] Agregar tests unitarios y E2E para los módulos nuevos.


### 🔧 2026-09-17 (noche) — Claude Sonnet 5 — Bug de fondo de "cajas fantasma" (arreglado, ver mesa de trabajo)

**Qué encontré**: el bug ya diagnosticado antes en este mismo archivo (puntero único `cajaActiva` en `useVentas.ts` vs. apertura de varias cajas a la vez desde `AperturaCajaModal`) seguía sin arreglar en el código — confirmado leyendo `cerrarCaja` línea por línea antes de tocar nada. Antes de asumir que hacía falta limpiar cajas viejas en Supabase, verifiqué la tabla `caja`: las 280 filas ya estaban en `estado='cerrada'` (0 abiertas) — alguien ya las cerró en bloque hoy (dos cierres masivos, 02:39:53 y 04:58:41 UTC) antes de que yo revisara. No hice ninguna limpieza SQL porque no hacía falta — solo lo verifiqué, no lo asumí.

**Por qué importaba**: aunque las 69 cajas viejas ya estaban cerradas, el bug de código seguía activo — cualquier apertura múltiple futura iba a volver a generar cajas fantasma, y `ControlCaja.tsx` cerraba siempre `cajaActiva` (la de ESTE dispositivo) aunque el usuario tuviera seleccionada otra caja en la lista (`cajaVista`).

**Qué hice** (cambio mínimo, aditivo, AUTORIZO de Gonzalo recibido):
- `src/hooks/useVentas.ts`: `cerrarCaja` ahora acepta un 5º parámetro opcional `cajaId`. Sin ese parámetro se comporta EXACTAMENTE igual que antes (cierra `cajaActiva`). Con `cajaId`, cierra esa sesión específica de `sesionesCaja` sin tocar `cajaActiva` si es otra caja.
- `src/pages/ControlCaja.tsx`: el modal de cierre (`CierreCajaModal`) ahora recibe `cajaVista` (la caja realmente seleccionada en pantalla, variable que YA existía en el archivo) en vez de forzar siempre `cajaActiva`, y le pasa su `id` a `onCerrarCaja`.
- `src/App.tsx` (protegido, AUTORIZO recibido): una sola línea — el `onCerrarCaja` de `ControlCaja` ahora reenvía el `cajaId` a `cerrarCaja(...)`. La línea de `Ventas.tsx` (que usa `nota` en vez de `cajaId`) no se tocó — no lo necesitaba.

**Cómo lo verifiqué**: releí las 3 líneas/bloques exactos guardados en disco después de escribir (no solo confié en que el editor no dio error). Corrí `npx tsc --noEmit -p tsconfig.app.json` antes y después: el repo ya tenía ~1810 errores preexistentes de TypeScript (ver skill `dulce-placer-deuda-tecnica`, deuda técnica documentada, no relacionada). El primer intento sí introdujo 2 errores nuevos (usé el nombre `cajaSeleccionada` que no existe — el nombre real en el código es `cajaVista`); los corregí y la segunda corrida quedó con el mismo total de errores que antes de mi cambio (ni uno más, ni uno menos) — confirmado con `diff` línea por línea entre las dos corridas de `tsc`.

**Qué queda pendiente / qué NO hice**:
- No probado en dispositivo real todavía (abrir 2+ cajas, cerrar una específica desde la lista y confirmar que la otra sigue abierta) — falta build + `PUBLICAR_A_PRODUCCION.bat` y una prueba real de Gonzalo o de otro agente.
- No toqué `Ventas.tsx` ni su wiring de `onCerrarCaja` (línea 463 de `App.tsx`) — esa pantalla no tiene selector de "cuál caja cerrar", así que no lo necesitaba; si en el futuro se le agrega ese selector, aplicar el mismo patrón (`cajaId` opcional ya existe en `cerrarCaja`).
- El riesgo sistémico de "last-write-wins" en sesiones de caja (ya documentado arriba en este archivo) sigue sin resolver — este fix no lo toca.


✅ COMPLETADO — Claude Sonnet 5 — 2026-09-18 03:30 UTC — Fix de 3 de los 4 bugs encontrados en la auditoría en vivo de los 42 módulos (reportados a Gonzalo, autorización recibida: "si no [está arreglado], procede").

**Qué encontré**: Antes de tocar nada, verifiqué 3-5 veces (git log, git status, mtime de archivos, git diff) que ninguno de los 4 bugs había sido arreglado por el flujo de commits de otro agente esta noche, y que nadie tenía cambios sin commitear en curso sobre estos archivos. Confirmado: los 3 bugs de abajo seguían intactos.

1. `src/App.tsx`: faltaba el `lazy import` de `MonetizacionInfluencer` → esto rompía TODA la app (pantalla negra "Error Crítico") al entrar a "Tablero de Monetización".
2. `src/components/reportes/DiagnosticoFinanciero.tsx`: el componente usa `<TabsContent>` sin un `<Tabs>` padre cuando se abre standalone desde "Modo Ayudante" (vía `modoLibretaHorno`) → crash contenido (Error Boundary) en ese módulo.
3. `src/lib/database.ts`: faltaba el método `getAllFacturasEscaneadas()` que sí existe (como stub) en `supabase-db.ts` → rompía la carga de facturas en "Egresos y Facturas" y en el dispatcher de agentes.

**Por qué importa**: son 4 módulos de los 42 que mostraban pantallazos de error en producción, tal como reportó Gonzalo.

**Qué hice** (cambio mínimo en cada caso):
1. Agregué la línea `const MonetizacionInfluencer = lazy(() => import('@/pages/MonetizacionInfluencer'));` en el bloque de imports lazy de `App.tsx` (junto a los otros ~40, mismo patrón).
2. En `DiagnosticoFinanciero.tsx`: agregué `Tabs` al import de `@/components/ui/tabs`; renombré `return (` → `const contenidoTabs = (` antes del JSX; después del cierre agregué `return modoLibretaHorno ? <Tabs value="quincena">{contenidoTabs}</Tabs> : contenidoTabs;`. Así el modo standalone (Modo Ayudante) se envuelve en su propio `Tabs`, y el modo normal (dentro de Reportes, que ya tiene su propio `Tabs`) sigue exactamente igual que antes.
3. En `database.ts`: agregué `async getAllFacturasEscaneadas(): Promise<any[]> { return []; }` (mismo stub que ya existía en `supabase-db.ts`), justo después de `getAllProductos()`.

**Cómo lo verifiqué**: corrí `npx tsc --noEmit -p tsconfig.app.json` completo antes y después. Total de errores: 1804 (la deuda técnica documentada venía siendo ~1809-1811, así que no solo no subió, está dentro de lo esperado). Confirmé con `grep` que NINGÚN error menciona `MonetizacionInfluencer`, `getAllFacturasEscaneadas` ni `contenidoTabs` — los 3 fixes no introdujeron ni un solo error nuevo. No pude re-probar en vivo en `app-eight-sigma-13.vercel.app` porque el sitio de producción no se actualiza con cambios locales — requiere un build + deploy real (`PUBLICAR_A_PRODUCCION.bat`, que solo corre en la PC de Gonzalo).

**⚠️ Hallazgo adicional importante (no relacionado a estos bugs, NO LO TOQUÉ)**: al revisar `git status` antes de editar, encontré que prácticamente TODO el repo (862 de 1268 archivos) aparece como "modificado" en `git diff`, pero verifiqué archivo por archivo que es SOLO un cambio de fin de línea (LF → CRLF), no pérdida ni cambio real de código — comparé bytes de `deviceId.ts` entre HEAD y el working tree y el contenido es idéntico, solo los saltos de línea cambiaron. Esto probablemente viene de guardar archivos con un editor en Windows configurado a CRLF, con `core.autocrlf=false` en este repo. NO es una edición activa de otro agente ahora mismo (los mtimes están repartidos en varios días, no todos de hoy). No lo arreglé porque es una decisión de arquitectura/config del repo (agregar `.gitattributes` con `* text=auto eol=lf`, o cambiar la config del editor) que le corresponde decidir a Gonzalo — si alguien hace `git add -A && git commit` así como está ahora, va a generar un commit gigante de "862 archivos cambiados" que en realidad no cambia nada de código, solo ensucia el historial. Se lo voy a reportar directo a Gonzalo.

**Qué queda pendiente (NO LO TOQUÉ, requiere decisión)**:
- Bug #4 de la auditoría: `useAuditorias.ts` llama a `db.getAll()`, `db.add()` y `db.delete()` genéricos que NO existen en `database.ts` (`NexusDatabase` solo tiene métodos nombrados por entidad, ej. `getAllProductos()`). Esto rompe el historial de auditorías de producción. Es un hallazgo de arquitectura (afecta el diseño de la clase protegida `database.ts`), así que según la skill de deuda técnica lo dejo señalado para que Gonzalo decida entre: (a) agregar métodos CRUD genéricos a `NexusDatabase`, o (b) reescribir el hook para usar métodos nombrados. NO lo parcheé.
- La limpieza de fin de línea (CRLF) de todo el repo — ver hallazgo de arriba, pendiente de decisión de Gonzalo.
- Verificación EN VIVO de estos 3 fixes — pendiente de un build + deploy real desde la PC de Gonzalo (u otro agente con acceso a esa PC).
